# Lesson 2.14: HTTP/2 Support in httpx

## A. Concept Overview

HTTP/2 provides multiplexing, header compression, and better performance.

---

## B. Enabling HTTP/2

```python
@pytest.mark.asyncio
async def test_with_http2():
    async with httpx.AsyncClient(http2=True) as client:
        response = await client.get("https://httpbin.org/get")
        print(f"HTTP version: {response.http_version}")
        assert response.status_code == 200
```

---

## C. Benefits for Testing

- Multiple requests over single connection
- Reduced latency
- Better for microservices testing

---

## D. Key Takeaways

🔑 Enable with http2=True  
🔑 Server must support HTTP/2  
🔑 Better performance for many requests  

Ready for 2.15? 🚀
